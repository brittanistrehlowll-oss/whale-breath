# V5 scaffold row for dsh-jingxi. Register as a native package row so the
# client-modules node half discovers the dsh.client declaration and serves
# /plugins/dsh-jingxi/client.js into window.__DSH_BOOT__.
- insert:
    - id: jingxi
      name: dsh-jingxi
